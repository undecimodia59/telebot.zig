// Struct representing a created giveaway
pub const GiveawayCreated = struct {
    // Optional. The number of Telegram Stars to be split between giveaway winners;
    // for Telegram Star giveaways only
    prize_star_count: ?i32,
};
